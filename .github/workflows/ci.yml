name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - run: npm ci

      - run: npm run lint

      - run: npm run check

      - run: npm run test

      # `npm run api:types` is intentionally skipped — it hits a live
      # backend to regenerate src/lib/api/types.ts. The committed file
      # is the source of truth in CI.
      - run: npm run build

---
interface Props {
  class?: string;
  freshnessId?: string;
}

const { class: className, freshnessId = "site-footer-freshness" } = Astro.props;
---

<footer class:list={["site-footer", className]}>
  <span class="site-footer__slot" data-site-footer-freshness id={freshnessId}></span>
  <a href="https://ivotsonev.com" rel="noreferrer" target="_blank">
    Built by ivotsonev.com
  </a>
</footer>

<style is:global>
  .site-footer {
    display: flex;
    flex: 0 0 auto;
    min-height: 46px;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 14px;
    border-top: 1px solid var(--color-border-subtle);
    background: var(--color-bg-surface);
    color: var(--color-text-subtle);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
    font-size: 0.78rem;
    padding: 8px 16px;
  }

  .site-footer__slot {
    display: inline-flex;
    align-items: center;
    gap: 12px;
  }

  .site-footer__slot:empty {
    display: none;
  }

  .site-footer a {
    color: var(--color-accent);
    font-weight: 800;
    text-decoration: none;
  }

  .site-footer__separator {
    color: var(--color-text-faint);
    user-select: none;
  }

  @media (hover: hover) and (pointer: fine) {
    .site-footer a:hover {
      text-decoration: underline;
      text-underline-offset: 3px;
    }
  }
</style>
